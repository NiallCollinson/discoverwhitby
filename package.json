{
  "name": "discoverwhitby",
  "version": "0.1.0",
  "description": "Whitby Holiday Rentals – web application with Beds24 integration.",
  "private": true,
  "packageManager": "pnpm@9.7.1",
  "type": "module",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "scripts": {
    "build": "pnpm --filter @discoverwhitby/web build",
    "dev": "pnpm --filter @discoverwhitby/web dev",
    "lint": "pnpm --filter @discoverwhitby/web lint",
    "test": "pnpm --filter @discoverwhitby/web test",
    "format": "prettier -w ."
  },
  "workspaces": [
    "apps/*"
  ],
  "keywords": [],
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
